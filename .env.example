# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# Next Auth
# You can generate a new secret on the command line with:
# npx auth secret
# https://next-auth.js.org/configuration/options#secret
AUTH_SECRET=""
AUTH_URL=""

# Database (shared between Drizzle and Directus)
DB_PORT=54322
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=postgres
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres

MAIL_LOGIN=""
MAIL_LOGIN_PASSWORD=""

# Mastra / Google Gemini (use either key, GOOGLE_GENERATIVE_AI_API_KEY is preferred)
GOOGLE_GENERATIVE_AI_API_KEY=""

# Google OAuth for NextAuth
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# OLX.uz Partner API (issued by OLX after partner approval)
OLX_CLIENT_ID=""
OLX_CLIENT_SECRET=""
# Must exactly match a callback URL registered by OLX.
# Example: https://ats.example.com/api/integrations/olx/callback
OLX_REDIRECT_URI=""
# Optional fallback when the API-localized jobs root cannot be detected by name.
OLX_JOBS_CATEGORY_ID=""

# Absolute or relative (to project root) path for candidate resume files.
# Example: "./storage"
RESUME_STORAGE_PATH="./storage"

# Directus Storage
DIRECTUS_URL=""
DIRECTUS_TOKEN=""
DIRECTUS_FOLDER=""

# Directus Docker
DIRECTUS_SECRET=""
DIRECTUS_ADMIN_EMAIL=""
DIRECTUS_ADMIN_PASSWORD=""
VERCEL_URL=""
